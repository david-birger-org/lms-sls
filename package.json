{
  "name": "sls-lms",
  "type": "module",
  "scripts": {
    "dev:vercel": "vercel dev --debug 3001",
    "dev:vercel:listen": "vercel dev --listen 3001",
    "deploy": "bunx vercel deploy",
    "deploy:prod": "bunx vercel deploy --prod",
    "lint": "biome check",
    "format": "biome format --write",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.8",
    "@types/bun": "latest",
    "@types/qrcode": "^1.5.6",
    "typescript": "^5.9.3"
  }
}
